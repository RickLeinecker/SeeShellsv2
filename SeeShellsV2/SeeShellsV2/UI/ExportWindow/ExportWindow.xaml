<Window x:Class="SeeShellsV2.UI.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:converters="clr-namespace:SeeShellsV2.UI.Converters"
        xmlns:local="clr-namespace:SeeShellsV2.UI"
        xmlns:data="clr-namespace:SeeShellsV2.Data"
        mc:Ignorable="d"
        Height="450" Width="800">
	<Window.Resources>
		<converters:ModuleViewConverter x:Key="ModuleViewConverter"/>
	</Window.Resources>
    <DockPanel>
        <ItemsControl DockPanel.Dock="Top" ItemsSource="{Binding moduleList}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<Border>
						<ContentPresenter Content="{Binding Converter={StaticResource ModuleViewConverter}}"/>
					</Border>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Click="Export_Click">
                <TextBlock Text="{Binding Status}"/>
            </Button>
        </StackPanel>
	</DockPanel>
</Window>
