<UserControl x:Class="SeeShellsV2.UI.TimelineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SeeShellsV2.UI"
             xmlns:data="clr-namespace:SeeShellsV2.Data"
             xmlns:repo="clr-namespace:SeeShellsV2.Repositories"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d"
             Loaded="GenerateTimeline"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="ShellEventCard" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Verdana"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <DataTemplate  DataType="{x:Type data:ShellEvent}">
            <Border BorderBrush="Gainsboro" BorderThickness="1" CornerRadius="4,4,4,4">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#0F830C" Offset="0.0" />
                        <GradientStop Color="#0F730C" Offset="0.5" />
                        <GradientStop Color="#0F830C" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Style="{StaticResource ShellEventCard}" Margin="0,3,0,3" Text="{Binding TypeName}"/>
                    <TextBlock Style="{StaticResource ShellEventCard}" Margin="3,0,3,0" Text="{Binding TimeStamp}"/>
                    <TextBlock Style="{StaticResource ShellEventCard}" Margin="0,3,0,3" Text="{Binding Description}"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <ScrollViewer PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
        <StackPanel CanVerticallyScroll="True" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Height="Auto" Width="Auto" >
        <ListView ItemsSource="{Binding ShellEvents}"/>
    </StackPanel>
    </ScrollViewer>
</UserControl>
